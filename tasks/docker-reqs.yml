---
- name: "Install docker + reqs"
  apt: name="{{ item }}" update_cache=yes state=latest
  with_items:
    - docker-engine
    - python-pip
  tags:
    - apt

- name: "Install docker-py"
  pip:
    name: docker-py
    state: present
    version: 1.7.0
  tags:
    - pip